export interface Contact {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  phone?: string;
  title?: string;
  accountId: string;
  organizationId: string;
  createdAt: string;
  updatedAt: string;
}
